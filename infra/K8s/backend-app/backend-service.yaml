apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: backend
spec:
  selector:
    app: backend  # ← BATE com o label do deployment
  ports:
  - protocol: TCP
    port: 4000
    targetPort: 4000
  type: ClusterIP